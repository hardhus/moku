[package]
name = "moku-core"
version = "0.1.1"
edition = "2024"

[dependencies]
ratatui.workspace = true
crossterm.workspace = true
anyhow.workspace = true
async-trait.workspace = true
toml.workspace = true
directories.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
arc-swap.workspace = true
futures.workspace = true

sled.workspace = true
zeroize.workspace = true
argon2.workspace = true
aes-gcm.workspace = true
rand.workspace = true
secrecy.workspace = true

# URL-scheme validation in util::open_url — a defensive check before
# handing a remote (RSS feed) URL to the OS's default-handler mechanism.
url = "2"

# Per-module storage subkey derivation (storage/keys.rs) — same
# scoped-not-workspace pinning as moku-vault-fs/moku-vault-daemon's own
# HKDF usage.
hkdf = "0.12"
sha2 = "0.10"

[target.'cfg(windows)'.dependencies]
windows.workspace = true

[dev-dependencies]
tempfile.workspace = true
